{
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && mkdir -p public && cp -r frontend/dist/* public/ && cd backend && npm install && npm run build",
    "start": "cd backend && npm start"
  },
  "devDependencies": {
    "@types/node": "^22.5.4"
  }
}
